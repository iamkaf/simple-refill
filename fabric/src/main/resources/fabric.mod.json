{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${description}",
  "authors": ["${mod_author}"],
  "contact": {
    "homepage": "https://github.com/iamkaf/minirefill",
    "issues": "https://github.com/iamkaf/minirefill/issues",
    "sources": "https://github.com/iamkaf/minirefill"
  },
  "license": "${license}",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {
    "main": ["com.iamkaf.minirefill.fabric.MiniRefillFabric"]
  },
  "mixins": [
    "${mod_id}.mixins.json",
    "${mod_id}.fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": "*",
    "minecraft": "${fabric_version_range}",
    "java": ">=${java_version}",
    "amber": ">=${amber_version}",
    "konfig": "*"
  },
  "suggests": {
    "modmenu": ">=${mod_menu_version}"
  }
}
